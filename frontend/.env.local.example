# ATHENA Frontend — Environment Variables
# Copy this file to .env.local for local development.
# The .env.local file is gitignored by Next.js by default.

# ── Backend URL ──────────────────────────────────────────────────────────────
# URL of the ATHENA backend API server.
#
# In Docker Compose (default): Next.js rewrites /api/* → http://backend:8000/api/*
# Set NEXT_PUBLIC_API_URL if the backend is on a different host/port.
#
# For local development without Docker:
NEXT_PUBLIC_API_URL=http://localhost:8000
#
# For production deployment, set this to your deployed backend URL, e.g.:
# NEXT_PUBLIC_API_URL=https://api.your-domain.com
