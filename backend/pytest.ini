[pytest]
# asyncio_mode = auto enables @pytest.mark.asyncio on all async test functions
# automatically, without needing explicit markers on every test.
asyncio_mode = auto

# Test discovery paths
testpaths = tests

# Show short traceback by default (change to 'long' for debugging)
addopts = -v --tb=short
